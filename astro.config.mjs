// @ts-check
import { defineConfig } from 'astro/config';

// If you deploy to GitHub Pages under a subpath (e.g. username.github.io/ilab-website),
// uncomment and set `site` and `base`. Netlify/Vercel need no changes.
export default defineConfig({
  // site: 'https://your-username.github.io',
  // base: '/ilab-website',
});
